This directory is used for the map-files.

If you want to obtain maps manually, please visit the KFLog-Homepage.
To enable the automatic map download for the users, change the permissions
for this directory and its subdirectories. (chmod -R a+w .)

You can find the map-files at:

http://maproom.kflog.org/
